select max(volume), min(volume), avg(volume), sum(volume) from company;

select max(market_cap), min(market_cap), avg(market_cap), sum(market_cap) from company;

select max(last_sale), min(last_sale), avg(last_sale), sum(last_sale) from company;

select max(percent_change), min(percent_change), avg(percent_change), sum(percent_change) from company;


